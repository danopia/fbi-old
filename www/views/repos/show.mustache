{{# repo }}
<h2>{{{ full_link }}}</h2>
<hr />

<p>{{ title }} is a code repository which belongs to the {{{ project_link }}} project.</p>

{{# service }}
<p>The code exists on {{{ show_link }}}. You can download the source from the repo's <a href="{{ url_format }}">{{ title }} page</a>.</p>
{{/ service }}

{{/ repo }}
